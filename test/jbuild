(jbuild_version 1)

(executables
    ((libraries (irmin-server))
     (names (test))
     (modules (test))))

(alias
    ((name runtest)
     (package irmin-server)
     (deps (test.exe))
     (action (run ${<}))))
